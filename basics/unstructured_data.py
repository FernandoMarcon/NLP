# NPL Basics
## Pre-processing text data

import nltk

#--- Read in semi-structured text data
# Read in the raw text
  rawData = open('data/SMSSpamCollection.tsv').read()
  rawData[:500]
sdsdfsfds

# Tolkenization
import re

def tokenize(text):


data['body_text_tolkenized'] = data['body_text_clean'].apply(lambda x: tokenize(x.lower()))

# Remove stopwords
stopword = nltk.corpus.stopwords.words('english')
def remove_stopwords(tokenized_)
g
